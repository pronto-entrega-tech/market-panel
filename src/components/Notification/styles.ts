import styled from 'styled-components';
import * as mui from '@mui/material';

export const Notification = styled(mui.Snackbar)``;

export const IconButton = styled(mui.IconButton).attrs({
  color: 'inherit',
})`
  padding: 0.5px;
`;
